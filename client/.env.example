# API Configuration
# In development, Vite proxies /api requests to the .NET backend (port 5203).
# Override this only if you need to point to a different backend.
VITE_API_BASE_URL=/api

# App Configuration
VITE_APP_NAME=Employee Management System
